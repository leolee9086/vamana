### 建议名称：预计算向量范数

#### 问题描述：
在 `vamana-index.ts` 以及 `distance.ts` 中，欧几里得距离（`computeEuclideanDistance`）和余弦距离（`computeCosineDistance`）的计算都涉及到向量范数（或平方范数）的计算。这些范数在每次距离计算时都会重复计算，尤其是在大规模数据集和高维向量的情况下，这会带来显著的性能开销。

例如，在 `computeEuclideanDistance` 中：
```typescript
export function computeEuclideanDistance(vecA: Float32Array, vecB: Float32Array): number {
  let sum = 0;
  for (let i = 0; i < vecA.length; i++) {
    const diff = vecA[i] - vecB[i];
    sum += diff * diff;
  }
  return Math.sqrt(sum);
}
```
以及 `computeCosineDistance` 中：
```typescript
export function computeCosineDistance(vecA: Float32Array, vecB: Float32Array): number {
  let dotProduct = 0;
  let normA = 0;
  let normB = 0;
  for (let i = 0; i < vecA.length; i++) {
    dotProduct += vecA[i] * vecB[i];
    normA += vecA[i] * vecA[i]; // 这里计算了vecA的平方范数
    normB += vecB[i] * vecB[i]; // 这里计算了vecB的平方范数
  }
  const normProduct = Math.sqrt(normA) * Math.sqrt(normB);
  return normProduct === 0 ? 1 : 1 - (dotProduct / normProduct);
}
```
`normA` 和 `normB` 的计算是重复的。

#### 优化建议：
在 `insertNode` 函数中，当一个新向量被插入时，预先计算并存储其范数（或平方范数）。将这个预计算的值作为 `VamanaNode` 的一个属性存储起来。

例如，可以修改 `VamanaNode` 接口：
```typescript
export interface VamanaNode {
  vector: Float32Array;
  id: number;
  data: any;
  neighbors: number[];
  // 新增属性
  sqNorm?: number; // 存储向量的平方范数，用于欧几里得和余弦距离
}
```
然后在 `insertNode` 中计算 `sqNorm`：
```typescript
const newNode: VamanaNode = {
  vector: vectorArray,
  id: nodeId,
  data,
  neighbors: [],
  sqNorm: calculateSqNorm(vectorArray) // 新增计算
};
```
其中 `calculateSqNorm` 是一个辅助函数：
```typescript
function calculateSqNorm(vec: Float32Array): number {
  let sum = 0;
  for (let i = 0; i < vec.length; i++) {
    sum += vec[i] * vec[i];
  }
  return sum;
}
```
最后，修改 `computeEuclideanDistance` 和 `computeCosineDistance`，使其利用 `VamanaNode` 中存储的 `sqNorm`。

对于欧几里得距离，虽然直接使用平方范数不能完全避免循环，但对于余弦距离，可以显著减少计算量。

#### 预期收益：
*   **显著减少距离计算的CPU开销**：尤其是在高维向量和大量距离计算的场景下（如 `greedySearch` 和 `robustPruneStandard`），避免了重复的循环计算。
*   **提高缓存命中率**：预计算的值存储在节点对象中，访问时更可能命中CPU缓存。
*   **提升整体索引构建和搜索性能**。
